# Frontend service scaffold

This directory is reserved for the `frontend` service source code used in Phase 3.

Expected next steps:
- Add the application source files.
- Add or update runtime-specific test/lint commands.
- Keep `Dockerfile` aligned with your chosen stack.
