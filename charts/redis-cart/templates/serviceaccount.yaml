{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "redis-cart.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "redis-cart.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
