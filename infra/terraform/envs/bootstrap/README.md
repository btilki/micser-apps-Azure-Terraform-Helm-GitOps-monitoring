Apply once per subscription to create the Terraform remote state storage account and containers.

After apply, copy `backend.hcl.example` in `../shared` (and dev/stage/prod) with the real storage account and resource group names.
